PORT=5000
NODE_ENV=development
APP_BASE_URL=http://localhost:5000
DATABASE_URL="mongodb+srv://username:password@cluster.mongodb.net/ftcc_medical_mission?retryWrites=true&w=majority&serverSelectionTimeoutMS=5000"
FACEBOOK_GRAPH_VERSION=v23.0
FACEBOOK_PAGE_ID=61582781532976
FACEBOOK_PAGE_ACCESS_TOKEN=

# Optional: Telegram bot. Never commit real tokens.
TELEGRAM_BOT_TOKEN=
# Local dev only — Render enables polling automatically (RENDER=true).
# TELEGRAM_ENABLE_POLLING=true

# Local dev with Docker Compose (optional — run `docker compose up -d` from repo root):
# DATABASE_URL="mongodb://127.0.0.1:27017/ftcc_medical_mission?serverSelectionTimeoutMS=5000"
